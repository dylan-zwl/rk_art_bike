<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- goal setting -->
    <style name="goal_edit_style">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/goal_text_edit</item>
        <item name="android:textColorHint">@color/goal_text_edit</item>
        <item name="android:textSize">22sp</item>
        <item name="android:background">#00000000</item>
    </style>

    <style name="goal_edit_hint_style">
        <item name="android:paddingLeft">40px</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/goal_text_edit</item>
        <item name="android:textColorHint">@color/goal_text_edit</item>
        <item name="android:textSize">22sp</item>
    </style>

    <!-- 分类应用按钮 -->
    <style name="setting_left_but_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">25sp</item>
        <item name="android:background">@drawable/fitness_aim_bt_bg_selector</item>
        <item name="android:layout_marginTop">@dimen/DIMEN_5PX</item>
        <item name="android:layout_marginRight">@dimen/DIMEN_5PX</item>
        <item name="android:button">@null</item>
    </style>

    <!-- 机器设置_输入框背景 -->
    <style name="setting_admin_maintenance_edit_style">
        <item name="android:layout_width">@dimen/DIMEN_180PX</item>
        <item name="android:layout_height">@dimen/DIMEN_60PX</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/admin_maintenance_bg</item>
        <item name="android:textColor">@color/setting_text_color</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginRight">@dimen/DIMEN_53PX</item>
    </style>
    
     <!-- Progress Dialog Style -->
    <style name="ProgressDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    
    <style name="ListViewFastScrollThumb" parent="@android:style/Theme.Light.NoTitleBar.Fullscreen">
        <item name="android:fastScrollThumbDrawable">@drawable/group_listview_bscroll</item>
    </style>
    
     <!-- Alert Dialog Style -->
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>